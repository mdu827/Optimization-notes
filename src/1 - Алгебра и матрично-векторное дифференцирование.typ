#import "@preview/physica:0.9.8"

= 😳 Линейная алгебра и матрично-векторное дифференцирование
== Векторы и матрицы
- Вектором далее называем массив (ie набор) чисел:
$
  x ^ T = [x_1, x_2, dots x_n], x in RR^n
$
- Матрица - множество векторов(как столбцов и строк) в таблице, далее отдельные виды матриц обозначаются как:
  - Симметрическая (если $A^T = A$) $A in SS^n$
  -  Положительно определенная(полуопределенная): $forall x eq.not 0 =>  x^(T)A x > 0 (>=0)$. Тогда $A in SS_(++)^n (SS_(+)^n)$. Аналогично определяется и отрицательная определенность и множества $SS_(--)^n, SS_(-)^n$.
== Матричное умножение (matmul)
В стандартном виде умножение матриц имеет сложность $O(n^3)$, однако существует алгоритм Штрассена, позволяющий выполнять умножение с $O(n^(log_2 7))$.

*Матричная экспонента* $e^A = sum _(k=0)^infinity 1/k! A^k, e^(A+B) eq.not e^A e^B$ (следует из некоммутативности умножения матриц)
== Скалярное произведение (dot product)
Обозначение: $angle.l x, y angle.r = x^T y = sum x_i y_i = y^T x  = angle.l y, x angle.r$

Свойства: 
-  $angle.l x, y  angle.r= angle.l y,x angle.r$
-  $angle.l x, A y angle.r = angle.l A x, y angle.r$
Вычислительно скалярное произведение считать проще, чем матричное, а значит для матриц $A_1, A_2, A_3$ и вектора х $=>$  выражение $A_1 A_2 A_3 x$ счиатать лучше справа налево, а не наоборот.

== Норма
=== Вектора
 Количественная норма вектора, обозначается как $||x||$:
- $||alpha x|| = |alpha| ||x||$
- $||x+y|| <= ||x|| + ||y||$
- $||x|| = 0 => x = 0$
*Расстояние* между векторами: $d(x, y) = ||x-y||$

Зачастую используется *p-норма* (по-умолчанию p= 2): $||x||_p = root(p, sum_(i=0)^n |x_i|^p)$
=== Матричная норма
Аналогичная норма в пространстве матриц данного размера, самые часто используемые нормы:
- Норма Фробениуса: $||A||_F = sqrt(sum_(i=1)^m sum_(j=1)^n |a_(i j)|^2)$
- Спектральная норма $||A||_2 = sup_{x eq.not 0} (||A x||_2) / (||x||_2)$. Также $||A||_2 = sigma_1(A) = sqrt(lambda_max(A^T A))$ ($sigma_1(A)$ - максимальное сингулярное значение, может быть получено из синглуярного SVD разложения, например)


При этом для вектора $x=> x^T x eq.not x x^T$, поэтому будем использовать обозначение *внутреннего* (ie скалярного, inner/dot product) произведения, если результат - действительное число, и *внешнего* (ie тензорного, outer product), если результат будет матрицей/тензором.

Стандартное (фробениусово) *скалярное произведение матриц* $X,Y in RR^(m times n)$: $angle.l X, Y angle.r = tr(X^ T Y) = sum_i sum_j X_(i j) Y_(i j) = tr(Y^T X) = angle.l Y, X angle.r$. Из него следует $angle.l X X angle.r = ||X||_F^2$
== Собственные векторы и собственные значения
$lambda$ - Собственное значение $A in RR ^(n times n)$, если $exists q eq.not 0: A q = lambda q$


*Теорема*: если А $succ.eq(succ)$ (ie Положительно полуопределенная или положительно определенная) $<=>$ все собственные значения неотрицательные (положительные)

== Матричные разложения
=== Спектральное разложение $A in SS_n$
Такая матрица А м/б представлена  в виде:
$
  A = Q Lambda Q^T
  \
  Lambda = text("diag")(lambda_1, lambda_2, ... , lambda_n)
  \
  Q = (q_1, q_2, ..., q_n)
$
где $lambda_i$ - собственное значение связанное с собственным веткором $q_i$. При этом Q - ортогональная, т е $Q^T Q = I$
=== Синглярное (SVD) разложение $A in RR^(m times n): text("rk")A = r$
Представление любой матрицы в виде произведения трех:
$
  A = U Sigma V^T
$

  где U, V - ортогональные, а $Sigma = text("diag")(sigma_1, sigma_2, ... sigma_r): sigma_1 >= sigma_2>= dots sigma_r$.  Столбцы U называют левыми сингулярными векторами, столбцы V - правыми.
  Сингулярные значения в $Sigma: sigma(A) = sqrt(lambda(A^T A))$. Исходная матрица А преставима в виде суммы матриц ранга 1:
  $
    A = sum_(i =1)^r sigma_i u_i v_i^T 
  $
  Здесь используется именно внешнее произведение векторов u, v.

Далее важную роль играет *число обусловленности* матрицы $k = ||A||  dot ||A^(-1)||$
также $k(A) = (sigma_(max) (A))/(sigma_(min) (A))$, если же $A in SS _(++)^n=> k(A) =  (lambda_(max) (A))/(lambda_(min) (A))$
== Определитель и след
- $det A = product_i^n lambda_i$
- $det(A B) = det A det B$
- $det A^(-1) = 1/det(A)$
- $tr A = sum _i ^n lambda_i$
- $tr(A B C D) = tr(D A B C) = tr(C D A B) = tr(B C D A)$

== Аппроксимация Тейлора 1-го порядка
Пусть:
- $f$ - дифференцируемая функция $RR_n -> RR$
- $nabla f(x_0) - $ градиент в точке $x_0$
Тогда можно линейно приблизить функцию $f$ вблизи $x_0$:
$
  f^I_(x_0)(x) = f(x_0) + nabla f(x_0)^T (x-x_0)
$
*Квардратичная аппроксимация *(2-го порядка):
$
   f^(I I)_(x_0)(x) = f(x_0) + nabla f(x_0)^T (x-x_0)+ 1/2(x-x_0)^T nabla^2f(x_0)(x-x_0)
$
где $nabla^2f(x_0)$ - матрица Гессе в точке $x_0$.

== Матричное дифференцирование
Пусть $f: RR^n -> R$, определим:
- *Градиент *$nabla f(x) = ( (partial f)/ (partial x_1), (partial f)/ (partial x_2), dots, (partial f)/ (partial x_n))$
- *Матрица Гессе/гессиан*:

Матрица Гессе функции $f(x, y)$:
$ H(f) = nabla^2 f = (diff^2f)/(diff x_i diff x_j)=  mat(
  frac(diff^2 f, diff x_1^2), frac(diff^2 f, diff x_1 diff x_2), dots.c, frac(diff^2 f, diff x_1 diff x_n);
  frac(diff^2 f, diff x_2 diff x_1), frac(diff^2 f, diff x_2^2), dots.c, frac(diff^2 f, diff x_2 diff x_n);
  dots.v, dots.v, dots.down, dots.v;
  frac(diff^2 f, diff x_n diff x_1), frac(diff^2 f, diff x_n diff x_2), dots.c, frac(diff^2 f, diff x_n^2)
) $
Если $H succ 0 => f$ - выпуклая, если $H prec 0 => f$ - вогнутая.
- *Якобиан*: если $f: RR^n -> RR^m$ матрица Якоби имеет вид:
$ J(f, x) = (d f)/ (d x) = mat(
  frac(∂ f_1, ∂ x_1), frac(∂ f_1, ∂ x_2), dots.v, frac(∂ f_1, ∂ x_n);
  frac(∂ f_2, ∂ x_1), frac(∂ f_2, ∂ x_2), dots.v, frac(∂ f_2, ∂ x_n);
  dots.v, dots.v, dots.down, dots.v;
  frac(∂ f_m, ∂ x_1), frac(∂ f_m, ∂ x_2), dots.v, frac(∂ f_m, ∂ x_n)
) $
=== Обозначения 
Если $f(x): X -> Y, (diff f)/ (diff x) in G$
- $X = RR, Y = RR => G = RR$ - производная $f'(x)$
- $X = RR^n, Y = RR => G = RR^n$ - градиент $(diff f)/ (diff x)$
- $X = RR^n, Y = RR^m => G = RR^(m times n)$ - якобиан $(diff f_i)/ (diff x_j)$
- $X = RR^(m times n), Y = RR => G = RR^(m times n)$ - матричная производная $(diff f)/ (diff x_(i j)$
#line(length: 100%)
== Дифференциалы
Линейная часть приращения функции:
$
  d f(x)= lim _(||d x|| -> 0) f(x+d x)- f(x) = angle.l nabla f(x), d x angle.r
$
Свойства дифференциалов:
- $d(alpha X) = alpha d X $ 
- $d(A X B) = A (d X ) B$ 
- $d angle.l X, Y angle.r =  angle.l d X, Y angle.r +  angle.l X, d Y angle.r$ 
- $d (X / phi)=  (phi (d X) - (d phi) X) / (phi ^ 2)$ 
- $d (det X)=  det X angle.l X^(-T), d X angle.r$ 
- $d (tr X)=  angle.l I, d X angle.r$ 
- $d f(g(X))=  (d f)/ (d g) d g(x)$ 


*Пример 1:* $f(x) = angle.l x , A x angle.r - b^T x + c$

$d f = d (angle.l x , A x angle.r - b^T x + c) = d (angle.l A x , x angle.r ) - d(angle.l b, x angle.r ) + d c = 
\
= angle.l A d x, x angle.r  + angle.l A x , d x angle.r  - angle.l b , d x angle.r  = angle.l A^T x, d x angle.r + angle.l A x, d x angle.r  - 
\
-angle.l b, d x angle.r = angle.l (A^T + A) x - b, d x angle.r => nabla f = (A^T + A)x - b $

*Пример 2:* $f(x) = ln (angle.l x , A x angle.r )$

$d f = (d angle.l x , A x angle.r )/ (angle.l x , A x angle.r ) = (angle.l (A + A^T) x , d x angle.r )/ (angle.l A x ,  x angle.r )$

В силу неотрицательности аргумента логарифма $=> A in SS_(++)^n$(положительно определенная матрица)

$d f = angle.l (2A x)/ (angle.l A x ,  x angle.r ),  d x angle.r )=> nabla f  = (2A x)/ (angle.l A x ,  x angle.r )$

*Пример 3 (матричная производная):* $f(x) = angle.l S , X angle.r  - ln det X$

$d f = d (angle.l S , X angle.r  - ln det X) = d (angle.l S , X angle.r)  - d(ln det X) = angle.l S , d X angle.r -
\
- (d det X)/(det X) = angle.l S , d X angle.r - (det X angle.l X^(-T), d X angle.r)/(det X) =  angle.l S - X^(-T) , d X angle.r 
\
=> nabla f = S - X^(-T)$
